using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mudarsemaforo : MonoBehaviour
{   /*
    Material mat;
    // Start is called before the first frame update
    void Start()
    {
        GameObject semaforo = GameObject.Find("Traffic_light_1");
        semaforo.GetComponent<Renderer>().setMaterials = mat;
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
